generated: '2026-08-13'
method: searched
source: https://github.com/Passionfroot/postgres-mcp/blob/main/examples/SKILL.md
note: >-
  Provider-published Agent Skill, saved VERBATIM (no frontmatter was added or altered).
  It is the companion skill to Passionfroot's first-party @passionfroot/postgres-mcp
  server and teaches an agent to look up schema before writing SQL. Scope caveat: like
  the MCP server itself, this skill operates against the OPERATOR'S OWN Postgres
  database — it is not an operating manual for a Passionfroot platform API, because
  Passionfroot publishes none. No skills were generated by this pipeline: there is no
  OpenAPI in this repo to ground a generated skill in, and inventing operations would
  be fabrication.
skills:
  - file: passionfroot-postgres-query.md
    name: postgres-query
    description: >-
      Assists with writing SQL queries for project databases. Orchestrates schema
      lookup via subagent to keep main context clean. Use when asked to query data,
      debug database issues, or explore the schema.
    api: null
    mcp: mcp/passionfroot-mcp.yml
    allowed_tools:
      - mcp__postgres__execute_sql
      - mcp__postgres__search_objects
    operations: []
    method: searched
    source: https://github.com/Passionfroot/postgres-mcp/blob/main/examples/SKILL.md
x-evidence:
  - {url: 'https://raw.githubusercontent.com/Passionfroot/postgres-mcp/main/examples/SKILL.md', http_status: 200}
